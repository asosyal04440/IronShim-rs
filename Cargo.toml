[package]
name = "ironshim-rs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[features]
default = []
alloc = []
loom = ["dep:loom"]
fuzzing = []

[dependencies]
loom = { version = "0.7", optional = true }
